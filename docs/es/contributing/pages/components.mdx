---
title: Componentes de MDX
noindex: true
sidebar_order: 40
---

<div id="alert">
  ## Alerta
</div>

Muestra una alerta (a veces llamada aviso) para destacar información importante.

<Alert title="Nota">
  Esta es una alerta informativa. Úsala para enfatizar información que los usuarios
  deberían leer.
</Alert>

<Alert level="warning" title="Importante">
  Esta es una alerta de advertencia. Úsala para alertar a los usuarios sobre posibles riesgos
  de los que deberían estar al tanto.
</Alert>

<Alert level="success" title="Consejo">
  Esta es una alerta de éxito. Úsala para proporcionar información opcional que puede
  ayudar a los usuarios a tener mejores resultados.
</Alert>

<Alert>
  Esta es una alerta sin título. Las alertas de `warning` siempre deben tener título
  para resaltar su importancia.
</Alert>

<Alert>
  Esta es una alerta de varias líneas sin título. Mantén la información breve y
  directa. Si esto resulta difícil, considera si el tema necesita documentación
  en otra página o si usar el componente [Expandable](#expandable) sería una
  mejor opción.
</Alert>

<Alert title="Lista">
  <ul>
    <li>Puedes crear listas</li>
    <li>y **formatear** *tu* `text`</li>
  </ul>
</Alert>

```markdown {tabTitle:Info}
<Alert title="Nota">
Esta es una alerta informativa.
</Alert>
```

```markdown {tabTitle:Warning}
<Alert level="warning" title="Importante">
Esta es una alerta de advertencia.
</Alert>
```

```markdown {tabTitle:Success}
<Alert level="success" title="Consejo">
Esta es una alerta de éxito.
</Alert>
```

```markdown {tabTitle:No Title}
<Alert>
Esta es una alerta informativa sin título.
</Alert>
```

Atributos:

* `title` (string) - opcional
* `level` (string: `'info' | 'warning' | 'success'`) - opcional, con valor predeterminado `'info'`

**Dónde colocar las alertas:**

Asegúrate de que tus alertas no interrumpan el flujo del contenido. Por ejemplo, no coloques una alerta entre dos párrafos que expliquen un mismo concepto. Usa las alertas con moderación; demasiadas en una sola página pueden resultar abrumadoras.

**Cuándo usar cada nivel de alerta:**

* **Info**: Úsala para destacar información que los usuarios deberían leer. Por ejemplo:
  * información necesaria para que los usuarios tengan éxito
  * información que normalmente incluirías en una nota

* **Warning**: Úsala para avisar a los usuarios sobre posibles riesgos que deben tener en cuenta. Por ejemplo:

  * advertencias sobre posibles errores, como los causados por descuidos habituales en la configuración del proyecto
  * advertencias sobre cambios que rompen la compatibilidad

  Buenas prácticas:

  * usa siempre un título con este nivel de alerta

* **Success**: Úsala para ofrecer información opcional que pueda ayudar a los usuarios a obtener mejores resultados. Por ejemplo:
  * información que ayuda a mejorar una tarea ya lograda con éxito, como un enlace a material adicional
  * consejos de buenas prácticas


<div id="arcade-embeds">
  ## Insertos de Arcade
</div>

Muestra un inserto de [Arcade](https://arcade.software).

<Arcade src="https://demo.arcade.software/v7uhzmdV6Q5PDzoVPAE6?embed&show_copy_link=true" />

```markdown {tabTitle:Example}
<Arcade src="https://demo.arcade.software/v7uhzmdV6Q5PDzoVPAE6?embed&show_copy_link=true" />
```

Atributos:

* `src` (cadena) - la URL de la inserción de Arcade


<div id="expandable">
  ## Expandible
</div>

Muestra una sección expandible para ofrecer información adicional a los usuarios bajo demanda.

<Expandable title="Algo que vale la pena destacar">
  Esta es una sección expandible con estilo de alerta `'info'`. Úsala para ofrecer
  información adicional que es útil, pero no crucial.
</Expandable>

<Expandable permalink title="Con enlace permanente">
  Esta es una sección expandible con estilo de alerta `'info'` con un título
  enlazado.
</Expandable>

<Expandable title="Esto podría ser importante para algunos usuarios" level="warning">
  Esta es una sección expandible con estilo de alerta `'warning'`. Úsala para advertir a
  los usuarios sobre riesgos potenciales menores.
</Expandable>

<Expandable title="¿Quieres algunos consejos para mejorar algo?" level="success">
  Esta es una sección expandible con estilo de alerta `'success'`. Úsala para
  ofrecer información opcional que ayude a los usuarios a tener más éxito.
</Expandable>

<Expandable title="Expandible con un bloque de código">
  `js const foo = 'bar'; `
</Expandable>

```markdown {tabTitle:Example}
<Expandable title="Algo que vale la pena destacar">
  Esta es una sección expandible con estilo de alerta `'info'`.
</Expandable>
```

```markdown {tabTitle:Permalink}
<Expandable permalink title="Con enlace permanente">
  Esta es una sección expandible con un enlace permanente.
</Expandable>
```

```markdown {tabTitle:Warning}
<Expandable level="warning" title="Esto podría ser importante para algunos usuarios">
  Esta es una sección expandible con estilo de alerta `'warning'`.
</Expandable>
```

```markdown {tabTitle:Success}
<Expandable level="success" title="¿Quieres algunos consejos para mejorar algo?">
  Esta es una sección expandible con estilo de alerta `'success'`.
</Expandable>
```

Atributos:

* `title` (string)
* `permalink` (boolean) - opcional: envuelve el título en un enlace y lo muestra en la tabla de contenidos.

**Cuándo usar paneles desplegables:**

Los paneles desplegables ayudan a mantener la documentación despejada, permitiendo que las personas se centren en la información importante y ofreciéndoles la opción de explorar detalles adicionales relevantes.

Por ejemplo, úsalos para:

* ofrecer información útil para algunas personas, pero no para todas, como consejos de solución de problemas
* proporcionar información de contexto o perspectivas sobre conceptos relacionados

**Mejores prácticas**

* Escribe el título del panel como una pregunta o afirmación para aclarar qué pueden esperar las personas del contenido:
  * ¿Quieres saber más sobre estas funciones?
  * ¿Usas Node.js versión 15 o anterior?
  * Opciones de configuración avanzadas para el caso de uso XY.
* Cuando necesites compartir información importante que se deba leer, considera usar una [alerta](#alert) en su lugar, ya que su contenido es visible de forma predeterminada.
* Evita usar en exceso este componente y asegúrate de aportar información valiosa y relevante para el tema en cuestión.


<div id="code-blocks">
  ## Bloques de código
</div>

Los bloques de código consecutivos se contraerán automáticamente en un contenedor con pestañas. Este comportamiento suele ser útil si quieres definir un ejemplo en varios lenguajes:

````markdown {tabTitle:Example}
```javascript
function foo() {
  return "bar";
}
```

```python
def foo():
  return 'bar'
```
````

A veces puede que no quieras este comportamiento. Para solucionarlo, puedes separar los bloques de código con algo de texto adicional o usar el componente `<Break />`.

Además, los bloques de código también admiten las propiedades `tabTitle` y `filename`:

````markdown {tabTitle:Example}
```javascript {tabTitle: Hola} {filename: index.js}
var foo = "bar";
```
````

Puedes resaltar líneas específicas en un bloque de código usando `{fromLineA-toLineB}` para rangos,
o `{lineA,lineB}` para líneas concretas (o una combinación de ambos):

```javascript {2} {tabTitle:Example}
function foo() {
  let lookat = "me";
  return "bar";
}
```

````markdown {tabTitle:Source}
```javascript {2}
function foo() {
  let lookat = "me";
  return "bar";
}
```
````

También puedes resaltar diferencias usando el idioma `diff`:

````markdown {tabTitle:Example}
```diff
- menos uno
+ más uno
```
````

Si quieres mantener el resaltado de sintaxis, puedes añadir la metadata `diff` a cualquier bloque de código
y luego anotar el diff con `+` y `-`:

```javascript {diff} {tabTitle:Example}
function foo() {
-  return "bar";
+  return "baz";
}
```

````markdown {tabTitle:Source}
```javascript {diff}
function foo() {
-  return "bar";
+  return "baz";
}
```
````


<div id="sdkoption">
  ## SdkOption
</div>

Muestra un encabezado de opción de configuración del SDK con una tabla de metadatos. Los nombres de las opciones deben escribirse con la convención nativa de la plataforma (camelCase, PascalCase o snake&#95;case).

<Alert>
  Si se especifica `categorySupported`, ocultará automáticamente la opción cuando la plataforma no la admita.
</Alert>

```markdown {tabTitle:Example}
<SdkOption 
  name="sampleRate" 
  type="number" 
  defaultValue="1.0"
  envVar="SENTRY_SAMPLE_RATE">

La tasa de muestreo para eventos de error, expresada como un número entre `0.0` y `1.0` (inclusive).

</SdkOption>
```

Atributos:

* `name` (string) - **obligatorio** - el nombre de la opción con la nomenclatura nativa de la plataforma
* `type` (string) - opcional - tipo de dato (p. ej., `boolean`, `string`, `number`)
* `defaultValue` (string) - opcional - valor predeterminado de la opción
* `envVar` (string) - opcional - variable de entorno asociada
* `availableSince` (string) - opcional - versión del SDK en la que se introdujo la opción
* `categorySupported` (string[]) - opcional - categorías de plataforma que admiten esta opción (p. ej., `["browser", "server"]`)
* `categoryNotSupported` (string[]) - opcional - categorías de plataforma que no admiten esta opción

**Guías de nomenclatura nativa de la plataforma:**

* **camelCase**: JavaScript/TypeScript (todos los frameworks), Java, Kotlin, Dart/Flutter, Android, Apple (iOS/macOS)
* **PascalCase**: .NET (todos los frameworks), Unity, Unreal, PowerShell
* **snake&#95;case**: Python, PHP, Ruby, Go, Rust, Elixir, Native (C/C++), Godot

Usa el componente `<PlatformIdentifier name="option-name" />` para mostrar los nombres de las opciones con la nomenclatura correcta según el ajuste `case_style` de la plataforma.


<div id="pagegrid">
  ## PageGrid
</div>

Renderiza todos los `next_steps` de este documento o, en su defecto, las páginas hijas, incluyendo su `description` si está disponible.

Puedes especificar `next_steps` en el frontmatter de una página para incluirlos en la cuadrícula. Admite rutas relativas y las resolverá automáticamente.

```markdown {tabTitle:Example}
---
# en el frontmatter de una página:
next_steps:
  - ./child-one
  - ./child-two
  - ../parent/child-three
---

<PageGrid />
```

Atributos:

* `header` (string): valor de encabezado opcional para incluir, se muestra como un H2

* `nextPages` (boolean): muestra solo las páginas que siguen según el orden de la barra lateral

* `exclude` (string[]): una lista de páginas que se excluirán de la cuadrícula. Especifica el nombre de archivo de la página; por ejemplo, &quot;index&quot; para `index.mdx`.


<div id="platformcontent">
  ## PlatformContent
</div>

Renderiza una inclusión según el `platform` actualmente seleccionado en el contexto.

```markdown {tabTitle:Example}
<PlatformContent includePath="sdk-init" />
```

Atributos:

* `includePath` (string): la subcarpeta dentro de `/includes` a la que se va a asignar
* `platform` (string): de forma predeterminada toma el valor de `platform` del contexto de la página

Algunas notas:

* Cuando la plataforma actual proviene del contexto de la página y no se encuentra un include coincidente, el contenido se ocultará.

* Al igual que en `PlatformSection`, puedes insertar contenido en el bloque que se renderizará antes del include indicado, pero solo cuando haya un include disponible.

* Se usará un archivo llamado `_default` si no coincide ningún otro contenido.

Nota: Actualmente esto causa problemas con la generación de la tabla de contenidos (tableOfContents), por lo que se recomienda desactivar el TOC cuando se use.


<div id="platformidentifier">
  ## PlatformIdentifier
</div>

Muestra los términos con la capitalización correcta en el texto de la página (no en ejemplos de código) según la configuración de platform case&#95;style:

```markdown {tabTitle:Example}
<PlatformIdentifier name="before-send" />
```

Por ejemplo, si usas `<PlatformIdentifier name="before-send" />`, se renderizará como:

* `beforeSend` si case&#95;style=camelCase
* `before_send` si case&#95;style=snake&#95;case
* `BeforeSend` si case&#95;style=PascalCase

Este componente solo funciona correctamente en páginas de plataforma.


<div id="platformlink">
  ## PlatformLink
</div>

Útil para enlazar contenido específico de una plataforma cuando aún no hay una plataforma concreta seleccionada.

```markdown {tabTitle:Example}
<PlatformLink to="/enriching-events/" />
```

Esto llevará a los usuarios a una página donde podrán elegir la plataforma y, luego, al enlace correspondiente. Si ya están en una página con una plataforma activa, simplemente los llevará a la página adecuada y omitirá la redirección.


<div id="platformsection">
  ## PlatformSection
</div>

Renderiza una sección según la `platform` seleccionada actualmente en el contexto. Cuando la plataforma no sea válida, el contenido se ocultará.

```markdown {tabTitle:Example}
<PlatformSection notSupported={["javascript"]}>

Algo que se aplica a todas las plataformas, excepto JavaScript o Node.

</PlatformSection>
```

Atributos:

* `platform` (string) - usa por defecto el valor de `platform` del contexto de la página
* `supported` (string[])
* `notSupported` (string[])
* `noGuides` (boolean) - oculta esto en todas las guías (tiene prioridad sobre `supported`/`notSupported`)


<div id="onboarding-options">
  ## Opciones de onboarding
</div>

Si estás escribiendo documentación específica sobre funciones del producto, puedes incluir marcadores dentro de bloques de código para habilitar o deshabilitar ciertas partes de los fragmentos:

<Alert title="warning">
  No copies el siguiente fragmento de código: los marcadores `___PRODUCT_OPTION_START___` y
  `___PRODUCT_OPTION_END___` incluyen un carácter adicional (espacio de ancho cero)
  como solución temporal para que se muestren correctamente en el ejemplo.
</Alert>

````markdown
```go
  // _​__PRODUCT_OPTION_START___ performance
  // tu código aquí
  // _​__PRODUCT_OPTION_END___ performance
```
````

La sintaxis utiliza el estilo de comentarios estándar del lenguaje de programación que estás documentando. Por ejemplo:

* TypeScript/JavaScript: `// ___PRODUCT_OPTION_START___ feature`
* Python: `# ___PRODUCT_OPTION_START___ feature`

donde `feature` es el identificador de la característica (p. ej., `performance`, `profiling` o `session-replay`).

La visibilidad del bloque se controlará mediante el componente `OnboardingOptionButtons`:

```jsx diff
<OnboardingOptionButtons
  options={["error-monitoring", "profiling", "performance"]}
/>
```

* `options` puede ser un objeto con esta estructura:

```typescript
{
  id: 'error-monitoring' | 'performance' | 'profiling' | 'session-replay',
  disabled: boolean,
  checked: boolean
}
```

o una cadena (uno de estos `id` 👆) para mayor comodidad al usar valores predeterminados.

<Alert level="warning" title="Importante">
  La implementación subyacente depende de que los metadatos `onboardingOptions` en
  los bloques de código tengan una sintaxis JSON válida.
</Alert>

* valores predeterminados: `checked: false` y `disabled: false` (`true` para `error-monitoring`).

Ejemplo (resultado de lo anterior):

<OnboardingOptionButtons options={["error-monitoring", "performance", "profiling"]} dontStick />

```go
import (
	"fmt"
	"net/http"

	"github.com/getsentry/sentry-go"
	sentrygin "github.com/getsentry/sentry-go/gin"
	"github.com/gin-gonic/gin"
)

// Para inicializar el manejador de Sentry, primero debes inicializar Sentry
if err := sentry.Init(sentry.ClientOptions{
	Dsn: "___PUBLIC_DSN___",
  // ___PRODUCT_OPTION_START___ performance
	EnableTracing: true,
	// Establece TracesSampleRate en 1.0 para capturar el 100%
	// de las transacciones para monitoreo de rendimiento.
	// Recomendamos ajustar este valor en producción,
	TracesSampleRate: 1.0,
  // ___PRODUCT_OPTION_END___ performance
  // Agrega encabezados de solicitud e IP de usuarios,
  // visita: https://docs.sentry.io/platforms/go/data-management/data-collected/ para más información
  SendDefaultPII: true,
}); err != nil {
	fmt.Printf("Falló la inicialización de Sentry: %v\n", err)
}

// Luego crea tu aplicación
app := gin.Default()

// Una vez hecho, puedes adjuntar el manejador como uno de tus middleware
app.Use(sentrygin.New(sentrygin.Options{}))

// Configura las rutas
app.GET("/", func(ctx *gin.Context) {
	ctx.String(http.StatusOK, "¡Hola mundo!")
})

// Y ejecútala
app.Run(":3000")
```

Puedes mostrar contenido de forma condicional según las opciones de onboarding seleccionadas usando el componente
`OnboardingOption`

O puedes usar la prop `hideForThisOption` para ocultar el contenido de la opción seleccionada.

````markdown
```jsx
<OnboardingOption optionId="profiling" hideForThisOption>
  Oculta esta sección para la opción `profiling`.
</OnboardingOption>
```
````

Ejemplo:

* activa la opción `performance` arriba para ver el efecto:

  <OnboardingOption optionId="performance">
    ```jsx
    <OnboardingOption optionId="performance">
      // Este bloque de código está contenido en un componente `OnboardingOption` y solo
      // se mostrará cuando la opción `performance` esté seleccionada.
    </OnboardingOption>
    ```
  </OnboardingOption>

* activa la opción `profiling` arriba para ver el efecto:

  <OnboardingOption optionId="profiling" hideForThisOption>
    ```jsx
    <OnboardingOption optionId="profiling" hideForThisOption>
      // Este bloque de código está contenido en un componente `OnboardingOption` y solo
      // se mostrará cuando la opción `profiling` NO esté seleccionada.
    </OnboardingOption>
    ```
  </OnboardingOption>
