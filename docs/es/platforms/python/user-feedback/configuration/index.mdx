---
title: Configuración
description: "Obtén información sobre los campos generales de configuración de User Feedback."
sidebar_order: 6100
---

<div id="crash-report-modal">
  ## Modal de informe de fallos
</div>

Puedes personalizar el modal de informe de fallos según las necesidades de tu organización, por ejemplo, con fines de localización. Todas las opciones se pueden pasar mediante la llamada `Sentry.showReportDialog`.

| Param            | Default                                                                                           |
| ---------------- | ------------------------------------------------------------------------------------------------- |
| `eventId`        | Establece manualmente el ID del evento.                                                           |
| `dsn`            | Establece manualmente el DSN al que se reportará.                                                 |
| `user`           | Establece manualmente los datos del usuario _[un objeto con las claves que se indican a continuación]_. |
| `user.email`     | Dirección de correo electrónico del usuario.                                                      |
| `user.name`      | Nombre del usuario.                                                                               |
| `lang`           | _[automático]_ – **anula el código de idioma de Sentry**                                          |
| `title`          | Parece que estamos teniendo problemas.                                                            |
| `subtitle`       | Nuestro equipo ha sido notificado.                                                                |
| `subtitle2`      | Si quieres ayudar, cuéntanos abajo qué pasó. – **no visible en resoluciones de pantalla pequeñas** |
| `labelName`      | Nombre                                                                                            |
| `labelEmail`     | Correo electrónico                                                                                |
| `labelComments`  | ¿Qué pasó?                                                                                        |
| `labelClose`     | Cerrar                                                                                            |
| `labelSubmit`    | Enviar                                                                                            |
| `errorGeneric`   | Se produjo un error desconocido al enviar tu informe. Inténtalo de nuevo.                         |
| `errorFormEntry` | Algunos campos no eran válidos. Corrige los errores e inténtalo de nuevo.                         |
| `successMessage` | Tus comentarios se han enviado. ¡Gracias!                                                         |
| `onLoad`         | n/a - **un callback opcional que se invocará cuando se abra el widget**                           |
| `onClose`        | n/a - **un callback opcional que se invocará cuando se cierre el widget**                         |

El callback opcional `onLoad` se ejecutará cuando los usuarios vean el widget. Puedes usarlo para ejecutar lógica personalizada, por ejemplo, para registrar un evento de analítica:

<PlatformContent includePath="user-feedback/example-widget-onload" />

El callback opcional `onClose` se ejecutará cuando los usuarios cierren el widget. Puedes usarlo para ejecutar lógica personalizada, por ejemplo, para recargar la página:

<PlatformContent includePath="user-feedback/example-widget-onclose" />