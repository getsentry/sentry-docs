---
title: Compatibilidad con OpenTelemetry
description: "Uso de OpenTelemetry con Sentry Performance."
sidebar_order: 20
---

Puedes configurar tu [SDK de OpenTelemetry](https://opentelemetry.io/) para enviar trazas y spans a Sentry.

<div id="install">
  ## Instalación
</div>

<PlatformContent includePath="performance/opentelemetry-install" />

<div id="usage">
  ## Uso
</div>

<PlatformContent includePath="performance/opentelemetry-setup" />

<div id="opentelemetry-and-sentry">
  ## OpenTelemetry y Sentry
</div>

Con el SDK de OpenTelemetry de Sentry, un `Span` de OpenTelemetry se convierte en una `Transaction` o un `Span` de Sentry. El primer `Span` que pasa por el `SpanProcessor` de Sentry es una `Transaction`, y cualquier `Span` hijo se adjunta a la primera `Transaction` tras comprobar el contexto del `Span` padre. Esto aplica tanto al `Span` raíz de OpenTelemetry como a cualquier `Span` de nivel superior del sistema. Por ejemplo, una solicitud enviada del frontend al backend creará un `Span` raíz de OpenTelemetry con una `Transaction` de Sentry correspondiente. La solicitud en el backend creará una nueva `Transaction` de Sentry para el `Span` de OpenTelemetry. La `Transaction` y el `Span` de Sentry están vinculados como un trazo para fines de navegación y seguimiento de errores.

<div id="additional-configuration">
  ## Configuración adicional
</div>

Si necesitas un control más preciso de Sentry, consulta la <PlatformLink to="/configuration/">página de configuración</PlatformLink>. Si quieres filtrar transacciones antes de enviarlas a Sentry (para eliminar, por ejemplo, las comprobaciones de estado), puede resultarte útil la <PlatformLink to="/configuration/filtering/#filtering-transaction-events">página de filtrado</PlatformLink>.