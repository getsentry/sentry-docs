---
title: Serverless
description: "Obtén más información sobre la compatibilidad con serverless, disponible de forma nativa."
sidebar_order: 6
---

<div id="source-context">
  ### Contexto de origen
</div>

El SDK `sentry-go` incluye compatibilidad con soluciones serverless de forma nativa.
Sin embargo, para que los contextos de origen sean correctos, debes empaquetar tu código fuente junto con el binario.

Por ejemplo, al usar **AWS Lambda** y con la siguiente estructura de directorios:

```bash
.
├── bin
│   └── upload-image
│   └── process-image
│   └── create-thumbnails
├── functions
│   └── upload-image
│       └── main.go
│   └── process-image
│       └── main.go
│   └── create-thumbnails
│       └── main.go
├── helper
│   ├── foo.go
│   └── bar.go
├── util
│   ├── baz.go
│   └── qux.go

```

Puedes compilar uno de los binarios y empaquetarlo junto con los archivos fuente necesarios usando este comando:

```bash
GOOS=linux go build -o bin/upload-image functions/upload-image/main.go && zip -r handler.zip bin/upload-image functions/upload-image/ helper/ util/
```

El único requisito es que ubiques el código fuente en la máquina de despliegue. Todo lo demás debería hacerse automáticamente mediante el SDK.


<div id="events-delivery">
  ### Entrega de eventos
</div>

La mayoría de las soluciones serverless (si no todas) no esperan respuestas de red antes de cerrar el proceso.
Por ello, debemos asegurarnos de que el evento se entregue a los servidores de Sentry.

Esto puede lograrse de dos maneras: usando el método [`sentry.Flush`](../../configuration/draining/) o cambiando el transporte a [`HTTPSyncTransport`](../../configuration/transports/#httpsynctransport).