---
title: "Huella del SDK"
description: "Aprende a sobrescribir la huella predeterminada en tu SDK."
sidebar_order: 30
---

Todos los eventos tienen una huella. Los eventos con la misma huella se agrupan en un issue.

De forma predeterminada, Sentry ejecuta uno de nuestros algoritmos de agrupación integrados para generar una huella basada en la información disponible en el evento, como `stacktrace`, `exception` y `message`. Para ampliar el comportamiento de agrupación predeterminado o cambiarlo por completo, puedes usar una combinación de las siguientes opciones:

1. En tu SDK, usando la huella del SDK, como se documenta a continuación
2. En tu proyecto, usando [Fingerprint Rules](/es/concepts/data-management/event-grouping/fingerprint-rules/) o [Stack Trace Rules](/es/concepts/data-management/event-grouping/stack-trace-rules/)

En los SDK compatibles, puedes sobrescribir la agrupación predeterminada de Sentry pasando el atributo de huella como un arreglo de cadenas. La longitud del arreglo de una huella no está restringida. Esto funciona de forma similar a la [funcionalidad de Fingerprint Rules](/es/concepts/data-management/event-grouping/fingerprint-rules/), que siempre está disponible y puede lograr resultados similares.

<div id="basic-example">
  ## Ejemplo básico
</div>

En el caso más simple, los valores se pasan directamente:

<PlatformContent includePath="set-fingerprint/basic" />

Puedes usar sustitución de variables para incorporar valores dinámicos en la huella que normalmente se calcula en el servidor. Por ejemplo, puedes añadir el valor `{{ default }}` para incluir en la huella todo el hash de agrupación que se genera de forma predeterminada. Estos valores son los mismos que en la creación de huellas del lado del servidor. Consulta [Variables](/es/concepts/data-management/event-grouping/fingerprint-rules/#variables) para obtener más información.

<div id="group-errors-with-greater-granularity">
  ## Agrupar errores con mayor granularidad
</div>

En algunos escenarios, querrás agrupar los errores con más detalle.

Por ejemplo, si tu aplicación consulta una interfaz RPC (Remote Procedure Call) o un servicio API externo (Application Programming Interface), la traza de pila suele ser la misma, incluso si la solicitud saliente es muy diferente.

El siguiente ejemplo dividirá aún más el grupo predeterminado que Sentry crearía (representado por `{{ default }}`), teniendo en cuenta algunos atributos del objeto de error:

<PlatformContent includePath="set-fingerprint/rpc" />

<div id="group-errors-more-aggressively">
  ## Agrupar errores de forma más agresiva
</div>

También puedes reemplazar por completo la agrupación de Sentry.

Por ejemplo, si un error genérico, como uno de conexión a la base de datos, presenta muchas trazas de pila diferentes y Sentry nunca las agrupa, puedes reemplazar la agrupación de Sentry omitiendo `{{ default }}` del arreglo:

<PlatformContent includePath="set-fingerprint/database-connection" />