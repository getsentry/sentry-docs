---
title: Dart
caseStyle: camelCase
supportLevel: production
sdk: sentry.dart
categories:
  - mobile
  - browser
  - server
---

En esta página te ayudamos a empezar con el SDK de Dart de Sentry.

<Alert>
  Si aún no tienes una cuenta y un proyecto de Sentry creados, ve a [sentry.io](https://sentry.io/signup/) y luego vuelve a esta página.
</Alert>

<div id="features">
  ## Funciones
</div>

Además de capturar errores, puedes supervisar las interacciones entre varios servicios o aplicaciones si [habilitas el tracing](/es/concepts/key-terms/tracing/).

Selecciona qué funciones de Sentry quieres instalar, además de Error Monitoring, para obtener a continuación las instrucciones correspondientes de instalación y configuración.

<Alert>
  Sentry también ofrece un [SDK de Flutter](/es/platforms/dart/guides/flutter) dedicado.
</Alert>

<div id="install">
  ## Instalación
</div>

<OnboardingOptionButtons
  options={[
'error-monitoring',
'performance'
]}
/>

Sentry recopila datos mediante un SDK dentro del tiempo de ejecución de tu aplicación.

```yml {filename:pubspec.yaml}
dependencies:
  sentry: ^{{@inject packages.version('sentry.dart') }}
```

<div id="configure">
  ## Configurar
</div>

Para capturar todos los errores, inicializa el SDK de Sentry para Dart lo antes posible.

```dart
import 'package:sentry/sentry.dart';

Future<void> main() async {
  await Sentry.init((options) {
    options.dsn = '___PUBLIC_DSN___';
    // Agrega encabezados de solicitud e IP de usuarios, para más información visita:
    // https://docs.sentry.io/platforms/dart/data-management/data-collected/
    options.sendDefaultPii = true;
    // ___PRODUCT_OPTION_START___ performance
    // Establece tracesSampleRate en 1.0 para capturar el 100% de las transacciones para el rastreo.
    // Recomendamos ajustar este valor en producción.
    options.tracesSampleRate = 1.0;
    // ___PRODUCT_OPTION_END___ performance
  });

  // también puedes configurar SENTRY_DSN, SENTRY_RELEASE, SENTRY_DIST y
  // SENTRY_ENVIRONMENT mediante variables de entorno de Dart (--dart-define)
}
```

<div id="verify">
  ## Verificar
</div>

Comprueba que tu app esté enviando eventos a Sentry añadiendo el siguiente fragmento, que incluye un error intencional. Deberías ver el error informado en Sentry en unos minutos.

```dart
import 'package:sentry/sentry.dart';

try {
  throw StateError('Error de prueba de Sentry');
} catch (exception, stackTrace) {
  await Sentry.captureException(
    exception,
    stackTrace: stackTrace,
  );
}
```

<div id="next-steps">
  ## Próximos pasos
</div>

* <PlatformLink to="/features">Conoce las características del SDK de Dart de Sentry</PlatformLink>
* <PlatformLink to="/tracing/instrumentation">Añade instrumentación de rendimiento a tu app</PlatformLink>