---
title: WinterCGFetch
description: "Crea spans y añade encabezados de trazas a las solicitudes fetch en el runtime Edge. (predeterminado)"
supported:
  - javascript.nextjs
  - javascript.nuxt
---

<Alert>
  Esta integración solo funciona en el runtime Edge.
</Alert>

*Nombre de importación: `Sentry.winterCGFetchIntegration`*

Esta integración está habilitada de forma predeterminada. Si quieres modificar tus integraciones predeterminadas, lee [esto](./../#modifying-default-integrations).

La `winterCGFetchIntegration` crea spans y añade encabezados de trazas a las solicitudes fetch en el runtime Edge.

```JavaScript
Sentry.init({
  integrations: [Sentry.winterCGFetchIntegration()],
});
```


<div id="options">
  ## Opciones
</div>

<div id="breadcrumbs">
  ### `breadcrumbs`
</div>

_Tipo: `boolean`_

Si se establece en `false`, no se capturarán las migas de pan.

<div id="shouldcreatespanforrequest">
  ### `shouldCreateSpanForRequest`
</div>

_Tipo: `(url: string) => boolean`_

Permite definir un método para determinar si crear o no spans para rastrear las solicitudes salientes a la URL indicada. De forma predeterminada, se crearán spans para todas las solicitudes salientes.