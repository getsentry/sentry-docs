---
title: GPUContext
excerpt: ""
description: "Añade el contexto del dispositivo GPU a los eventos. (predeterminado)"
sidebar_order: 10
---

Añade el contexto del dispositivo GPU a los eventos.

Esta integración admite varias opciones, que de forma predeterminada están establecidas en `true`:

```typescript
interface GPUContextOptions {
  /**
   * Cuánta información de la GPU solicitar a la API `app.getGPUInfo` de Electron.
   * `complete` puede tardar mucho más en resolverse, por lo que el valor predeterminado es `basic`.
   * - 'basic': Por lo general, solo `vendor_id` e `id`, aunque algunas plataformas proporcionan más.
   * - 'complete': Información más detallada, incluidos los nombres completos y `driver_version`.
   *
   * @default 'basic'
   */
  infoLevel: "basic" | "complete";
}
```

Para obtener información completa de la GPU, configura `infoLevel` en `complete`:

```javascript
import * as Sentry from "@sentry/electron/main";

Sentry.init({
  dsn: "___PUBLIC_DSN___",
  integrations: [
    Sentry.gpuContextIntegration({
      infoLevel: "complete",
    }),
  ],
});
```
