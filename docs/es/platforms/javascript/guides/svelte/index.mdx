---
title: Svelte
description: "Aprende a configurar Sentry manualmente en tu app de Svelte y a capturar tus primeros errores."
sdk: sentry.javascript.svelte
categories:
  - javascript
  - browser
---

<PlatformContent includePath="getting-started-prerequisites" />

<OnboardingSteps>
  <OnboardingOption isStep>
    ## Instalación

    Elige las funciones que quieres configurar y esta guía te mostrará cómo hacerlo:

    <OnboardingOptionButtons
      options={[
"error-monitoring",
"performance",
"session-replay",
"user-feedback",
"logs",
]}
    />

    <PlatformContent includePath="getting-started-features-expandable" />

    ### Instala el SDK de Sentry

    Ejecuta el comando correspondiente a tu gestor de paquetes preferido para añadir el SDK de Sentry a tu aplicación:

    ```bash {tabTitle:npm}
    npm install @sentry/svelte --save
    ```

    ```bash {tabTitle:yarn}
    yarn add @sentry/svelte
    ```

    ```bash {tabTitle:pnpm}
    pnpm add @sentry/svelte
    ```
  </OnboardingOption>

  <OnboardingOption isStep>
    ## Configurar

    Para usar el SDK, inicialízalo en el punto de entrada de Svelte antes de arrancar tu app. En un proyecto típico de Svelte, ese suele ser el archivo `main.js` o `main.ts`.

    ```javascript {tabTitle: Svelte v5+} {filename: main.js}
    import { mount } from "svelte";
    import "./app.css";
    import App from "./App.svelte";

    import * as Sentry from "@sentry/svelte";

    // Inicializa el SDK de Sentry aquí
    Sentry.init({
      dsn: "___PUBLIC_DSN___",

      // Añade encabezados de solicitud e IP para usuarios, para más información visita:
      // https://docs.sentry.io/platforms/javascript/guides/svelte/configuration/options/#sendDefaultPii
      sendDefaultPii: true,

      integrations: [
        // ___PRODUCT_OPTION_START___ performance
        Sentry.browserTracingIntegration(),
        // ___PRODUCT_OPTION_END___ performance
        // ___PRODUCT_OPTION_START___ session-replay
        Sentry.replayIntegration(),
        // ___PRODUCT_OPTION_END___ session-replay
        // ___PRODUCT_OPTION_START___ user-feedback
        Sentry.feedbackIntegration({
          // La configuración adicional del SDK va aquí, por ejemplo:
          colorScheme: "system",
        }),
        // ___PRODUCT_OPTION_END___ user-feedback
      ],
      // ___PRODUCT_OPTION_START___ logs

      // Habilita el envío de logs a Sentry
      enableLogs: true,
      // ___PRODUCT_OPTION_END___ logs

      // ___PRODUCT_OPTION_START___ performance
      // Establece tracesSampleRate en 1.0 para capturar el 100%
      // de las transacciones para el rastreo.
      // Recomendamos ajustar este valor en producción
      // Más información en
      // https://docs.sentry.io/platforms/javascript/configuration/options/#traces-sample-rate
      tracesSampleRate: 1.0,

      // Establece `tracePropagationTargets` para controlar en qué URLs debe habilitarse la propagación del rastreo
      tracePropagationTargets: ["localhost", /^https:\/\/yourserver\.io\/api/],
      // ___PRODUCT_OPTION_END___ performance
      // ___PRODUCT_OPTION_START___ session-replay

      // Captura Replay para el 10% de todas las sesiones,
      // más el 100% de las sesiones con error
      // Más información en
      // https://docs.sentry.io/platforms/javascript/session-replay/configuration/#general-integration-configuration
      replaysSessionSampleRate: 0.1,
      replaysOnErrorSampleRate: 1.0,
      // ___PRODUCT_OPTION_END___ session-replay
    });

    const app = mount(App, {
      target: document.getElementById("app"),
    });

    export default app;
    ```

    ```javascript {tabTitle: Svelte v3-v4} {filename: main.js}
    import "./app.css";
    import App from "./App.svelte";

    import * as Sentry from "@sentry/svelte";

    // Inicializa el SDK de Sentry aquí
    Sentry.init({
      dsn: "___PUBLIC_DSN___",
      integrations: [
        // ___PRODUCT_OPTION_START___ performance
        Sentry.browserTracingIntegration(),
        // ___PRODUCT_OPTION_END___ performance
        // ___PRODUCT_OPTION_START___ session-replay
        Sentry.replayIntegration(),
        // ___PRODUCT_OPTION_END___ session-replay
        // ___PRODUCT_OPTION_START___ user-feedback
        Sentry.feedbackIntegration({
          // La configuración adicional del SDK va aquí, por ejemplo:
          colorScheme: "system",
        }),
        // ___PRODUCT_OPTION_END___ user-feedback
      ],
      // ___PRODUCT_OPTION_START___ logs

      // Habilita el envío de logs a Sentry
      enableLogs: true,
      // ___PRODUCT_OPTION_END___ logs

      // ___PRODUCT_OPTION_START___ performance
      // Establece tracesSampleRate en 1.0 para capturar el 100%
      // de las transacciones para el rastreo.
      // Recomendamos ajustar este valor en producción
      // Más información en
      // https://docs.sentry.io/platforms/javascript/configuration/options/#traces-sample-rate
      tracesSampleRate: 1.0,

      // Establece `tracePropagationTargets` para controlar en qué URLs se debe habilitar la propagación del rastreo
      tracePropagationTargets: ["localhost", /^https:\/\/yourserver\.io\/api/],
      // ___PRODUCT_OPTION_END___ performance
      // ___PRODUCT_OPTION_START___ session-replay

      // Captura Replay para el 10% de todas las sesiones,
      // más el 100% de las sesiones con error
      // Más información en
      // https://docs.sentry.io/platforms/javascript/session-replay/configuration/#general-integration-configuration
      replaysSessionSampleRate: 0.1,
      replaysOnErrorSampleRate: 1.0,
      // ___PRODUCT_OPTION_END___ session-replay
    });

    const app = new App({
      target: document.getElementById("app"),
    });

    export default app;
    ```
  </OnboardingOption>

  <OnboardingOption isStep>
    ## Agrega trazas de pila legibles con mapas de origen (opcional)

    <PlatformContent includePath="getting-started-sourcemaps-short-version" />
  </OnboardingOption>

  <OnboardingOption isStep>
    ## Evitar los bloqueadores de anuncios con túneles (opcional)

    <PlatformContent includePath="getting-started-tunneling" />
  </OnboardingOption>

  <OnboardingOption isStep>
    ## Verifica tu configuración

    Probemos tu configuración y confirmemos que Sentry funciona correctamente y envía datos a tu proyecto de Sentry.

    ### Incidencias

    Para comprobar que Sentry captura errores y crea incidencias en tu proyecto de Sentry, agrega el siguiente botón de prueba a una de tus páginas; al hacer clic en él se producirá un error que Sentry capturará:

    ```jsx {tabTitle:Svelte v5+} {filename:SomeComponent.svelte}
    <button
      type="button"
      onclick={() => {
        throw new Error("Sentry Frontend Error");
      }}
    >
      Generar error
    </button>
    ```

    ```jsx {tabTitle:Svelte v3/v4} {filename:SomeComponent.svelte}
    <button
      type="button"
      on:click={() => {
        throw new Error("Sentry Frontend Error");
      }}
    >
      Generar error
    </button>
    ```

    <OnboardingOption optionId="performance" hideForThisOption>
      Abre la página en un navegador y haz clic en el Button para provocar un error en el frontend.
    </OnboardingOption>

    <PlatformContent includePath="getting-started-browser-sandbox-warning" />
  </OnboardingOption>

  <OnboardingOption optionId="performance">
    ### Tracing

    Para probar tu configuración de tracing, actualiza el fragmento de código anterior para iniciar una traza de rendimiento y medir el tiempo que tarda en ejecutarse tu código:

    ```jsx {tabTitle:Svelte v5+} {filename:SomeComponent.svelte}
    <button
      type="button"
      onClick={() => {
        Sentry.startSpan({ op: "test", name: "Example Frontend Span" }, () => {
          setTimeout(() => {
            throw new Error("Sentry Frontend Error");
          }, 99);
        });
      }}
    >
      Lanzar error
    </button>
    ```

    ```jsx {tabTitle:Svelte v3/v4} {filename:SomeComponent.svelte}
    <button
      type="button"
      on:click={() => {
        Sentry.startSpan({ op: "test", name: "Example Frontend Span" }, () => {
          setTimeout(() => {
            throw new Error("Sentry Frontend Error");
          }, 99);
        });
      }}
    >
      Lanzar error
    </button>
    ```

    Abre la página en un navegador y haz clic en el Button para provocar un error de frontend y un rastreo de rendimiento.
  </OnboardingOption>

  <OnboardingOption>
    ### Ver datos capturados en Sentry

    Ahora, ve a tu proyecto en [Sentry.io](https://sentry.io/) para ver los datos recopilados (pueden tardar un par de momentos en aparecer).

    <PlatformContent includePath="getting-started-verify-locate-data" />

    ## Próximos pasos

    A estas alturas, deberías haber integrado Sentry en tu aplicación Svelte y ya deberías estar enviando datos a tu proyecto de Sentry.

    Ahora es un buen momento para personalizar tu configuración y explorar temas más avanzados. Te recomendamos como siguientes pasos:

    * Amplía Sentry a tu backend usando uno de nuestros [SDK](/es/)
    * Continúa para <PlatformLink to="/configuration">personalizar tu configuración</PlatformLink>
    * Aprovecha las <PlatformLink to="/features">funciones específicas de Svelte</PlatformLink>
    * Aprende a <PlatformLink to="/usage">capturar errores manualmente</PlatformLink>

    <Expandable permalink={false} title="¿Tienes problemas para configurar el SDK?">
      - Encuentra varios temas en <PlatformLink to="/troubleshooting">Solución de problemas</PlatformLink>
      - [Obtén ayuda](https://sentry.zendesk.com/hc/en-us/)
    </Expandable>
  </OnboardingOption>
</OnboardingSteps>