---
title: "Jerarquía de vistas"
description: "Obtén más información sobre cómo depurar la jerarquía de vistas cuando ocurre un error. Sentry vincula la representación de la jerarquía de vistas con el evento original, dándote información adicional sobre los problemas."
supported:
  - apple.ios
  - apple.visionos
notSupported:
  - apple.macos
  - apple.tvos
  - apple.watchos
---

Sentry permite generar una representación JSON de la jerarquía de vistas de un error y la incluye como un <PlatformLink to="/enriching-events/attachments/">adjunto</PlatformLink>.

Esta función solo se aplica a los SDK con interfaz de usuario, como los de aplicaciones móviles y de escritorio. En algunos entornos, como iOS nativo, generar la jerarquía de vistas requiere el hilo de la UI y, en caso de un bloqueo, puede que no esté disponible. Otro ejemplo en el que la jerarquía de vistas podría no estar disponible es cuando el evento ocurre antes de que la pantalla empiece a cargarse. Por lo tanto, esta función es intrínsecamente una solución de “mejor esfuerzo”.

<Alert>
  Los eventos de bloqueo de la app no tendrán jerarquía de vistas porque el hilo principal está bloqueado y Sentry no puede interactuar con elementos de la UI en una vista en segundo plano.
</Alert>

<Alert>
  La desofuscación de jerarquías de vistas está totalmente admitida en los SDK nativos y en React Native, pero actualmente no está admitida en Flutter. Las jerarquías de vistas no son compatibles con SwiftUI.
</Alert>

<div id="enabling-view-hierarchy-attachments">
  ## Activar adjuntos de jerarquía de vistas
</div>

La depuración de la jerarquía de vistas es una función opcional. Puedes activarla como se muestra a continuación:

<PlatformContent includePath="enriching-events/attach-viewhierarchy" />

<div id="privacy-in-view-hierarchy-attachments">
  ## Privacidad en los adjuntos de la jerarquía de vistas
</div>

Sentry no recopila ninguna información del usuario (como el texto de UILabel y UITextView) con el adjunto de jerarquía de vistas, que solo incluye la estructura y las propiedades.

Si usas la propiedad `accessibilityIdentifier` de la vista con información personal, asegúrate de desactivar el envío de `accessibilityIdentifier`, como en el ejemplo siguiente:

```swift {tabTitle:Swift}
import Sentry
SentrySDK.start { options in
  options.attachViewHierarchy = true
  options.reportAccessibilityIdentifier = false
}
```

```objc {tabTitle:Objective-C}
@import Sentry;
[SentrySDK startWithConfigureOptions:^(SentryOptions *options) {
   options.attachViewHierarchy = YES;
   options.reportAccessibilityIdentifier = NO;
}];
```

<div id="viewing-view-hierarchy-attachments">
  ## Visualización de adjuntos de jerarquía de vistas
</div>

Las jerarquías de vistas aparecen en la pestaña &quot;Attachments&quot;, donde puedes ver todos los adjuntos y los eventos asociados. Haz clic en el ID del evento para abrir la página [Issue Details](/es/product/issues/issue-details) de ese evento en particular.

<Include name="common-imgs/viewhierarchy-list-example" />

En la página **Issue Details**, puedes interactuar con el adjunto de jerarquía de vistas en una sección llamada &quot;View Hierarchy&quot;. Esta sección representa el estado de tu aplicación en el momento del evento de error. Hay tres modos de visualización: vista en árbol, wireframe y vista detallada del nodo seleccionado. Puedes seleccionar nodos tanto en el árbol como en el wireframe para ver las propiedades recopiladas por el SDK. El SDK informa las siguientes claves para cada nodo de la vista: `alpha`, `visible`, `x`, `y`, `width`, `height`, `type` e `identifier`, si corresponde, aunque puede haber valores adicionales específicos del SDK. Esta función puede usarse como herramienta exploratoria para depurar problemas de diseño, visualizar contenido renderizado innecesariamente u obtener una mejor comprensión de la relación entre vistas.

<Include name="common-imgs/viewhierarchy-example" />