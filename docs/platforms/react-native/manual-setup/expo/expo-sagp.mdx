---
title: Sentry Android Gradle Plugin (AGP)
description: "Learn how to configure the Sentry Android Gradle Plugin to automatically upload source maps and debug symbols for your Expo project."
---

To enable the plugin modify your `app.json` and add the `enableAndroidGradlePlugin` option in the `@sentry/react-native/expo` plugin.

```javascript {tabTitle:app.json}
{
  "expo": {
...
    "plugins": [
      [
        "@sentry/react-native/expo",
        {
          "url": "https://sentry.io/",
          "project": "project-slug",
          "organization": "organization-slug",
          "experimental_android": {
            "enableAndroidGradlePlugin": true,
          }
        }
      ],
...
  }
}
```

## Configuration options

The supported [Sentry Android Gradle Plugin options](/platforms/android/configuration/gradle/) are the following:
- `autoUploadProguardMapping`
- `includeProguardMapping`
- `dexguardEnabled`
- `uploadNativeSymbols`
- `autoUploadNativeSymbols`
- `includeNativeSources`
- `includeSourceContext`


Those should be added in the `experimental_android` section.

```javascript {tabTitle:app.json}
{
  "expo": {
...
    "plugins": [
      [
        "@sentry/react-native/expo",
        {
...
          "experimental_android": {
            "enableAndroidGradlePlugin": true,
            "autoUploadProguardMapping": true,
            "includeProguardMapping": true,
            "dexguardEnabled": true,
            "uploadNativeSymbols": true,
            "autoUploadNativeSymbols": true,
            "includeNativeSources": true,
            "includeSourceContext": true,
          }
        }
      ],
...
  }
}
```
