<Alert>

This guide covers the Sentry SDK-based setup, which replaces the `flue add tooling sentry` blueprint. If you previously ran the blueprint, remove its `createOpenTelemetryInstrumentation` call — it emits its own `gen_ai` spans and will double-count against the ones the SDK creates.

</Alert>
