---
id: nextjs-with-sentry-config-entry
title: "`withSentryConfig` moved to `@sentry/nextjs/config`"
phase: packages
category: removed-api
severity: action-required
frameworks: [nextjs]
platformCategory: all
order: 75
---

```js {filename:next.config.mjs}
// Before
import { withSentryConfig } from "@sentry/nextjs";

// After
import { withSentryConfig } from "@sentry/nextjs/config";
```

The `SentryBuildOptions` type moved with it, and the no-op `withSentryConfig` passthroughs of the client and edge builds were removed.
