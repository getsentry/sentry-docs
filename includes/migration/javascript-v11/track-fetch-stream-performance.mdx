---
id: track-fetch-stream-performance
title: "`trackFetchStreamPerformance` moved to its own integration"
phase: code-changes
category: removed-api
severity: action-required
frameworks: all
platformCategory: browser
order: 230
---

The `trackFetchStreamPerformance` option of `browserTracingIntegration` was removed. Use `fetchStreamPerformanceIntegration()` instead.

```js
// Before
Sentry.init({
  integrations: [
    Sentry.browserTracingIntegration({
      trackFetchStreamPerformance: true,
    }),
  ],
});

// After
Sentry.init({
  integrations: [
    Sentry.browserTracingIntegration(),
    Sentry.fetchStreamPerformanceIntegration(),
  ],
});
```

See <PlatformLink to="/configuration/integrations/fetchstreamperformance/">FetchStreamPerformance</PlatformLink> for supported response types and setup instructions.
