---
id: inp-web-vital-span
title: "INP is always sent as a web vital span"
phase: verify
category: behavior
severity: behavior-change
frameworks: all
platformCategory: browser
order: 80
---

When collected, INP is always sent as a Web Vital span, carrying its value in the `browser.web_vital.inp.value` attribute instead of as a span measurement. Update custom dashboards and alerts that read it as a measurement. Built-in dashboards do not need adjustments.

See <PlatformLink to="/configuration/integrations/webvitals/">WebVitals</PlatformLink> for collection options and the attributes sent with each vital.
