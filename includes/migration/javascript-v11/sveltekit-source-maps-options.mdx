---
id: sveltekit-source-maps-options
title: "`sourceMapsUploadOptions` was removed from `sentrySvelteKit()`"
phase: code-changes
category: removed-api
severity: action-required
frameworks: [sveltekit]
platformCategory: all
order: 370
---

The `sourceMapsUploadOptions` option was removed. Move its fields to the top level of the SDK's build options:

```js
// Before
sourceMapsUploadOptions: {
  org: "my-org",
  project: "my-project",
  authToken: process.env.SENTRY_AUTH_TOKEN,
  sourcemaps: { assets: ["./build/**/*"] },
}

// After
org: "my-org",
project: "my-project",
authToken: process.env.SENTRY_AUTH_TOKEN,
sourcemaps: { assets: ["./build/**/*"] },
```

The `url` field is called `sentryUrl` at the top level.
