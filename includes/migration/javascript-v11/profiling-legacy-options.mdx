---
id: profiling-legacy-options
title: "`profilesSampleRate` and `profilesSampler` were removed"
phase: code-changes
category: removed-api
severity: action-required
frameworks: all
platformCategory: all
order: 250
---

The legacy per-transaction profiling options were removed. Configure <PlatformLink to="/profiling/">session-based profiling</PlatformLink> with `profileSessionSampleRate` and a `profileLifecycle` of `'trace'` or `'manual'` instead.
