---
id: koa-hapi-error-handlers
title: "`setupKoaErrorHandler` and `setupHapiErrorHandler` are deprecated"
phase: cleanup
category: behavior
severity: informational
frameworks:
  [
    astro,
    aws-lambda,
    azure-functions,
    bun,
    connect,
    effect,
    elysia,
    express,
    fastify,
    firebase,
    gcp-functions,
    hapi,
    hono,
    koa,
    mastra,
    nestjs,
    nextjs,
    nitro,
    node,
    nuxt,
    react-router,
    remix,
    solidstart,
    sveltekit,
    tanstackstart-react,
  ]
features: []
platformCategory: server
order: 50
---

The Koa and Hapi integrations capture errors automatically, so `setupKoaErrorHandler` and `setupHapiErrorHandler` no longer need to be called. Both are deprecated and will be removed in the next major version.

Remove the calls.
