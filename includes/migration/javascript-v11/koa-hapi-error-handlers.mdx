---
id: koa-hapi-error-handlers
title: "`setupKoaErrorHandler` and `setupHapiErrorHandler` are deprecated"
phase: cleanup
category: behavior
severity: informational
frameworks:
  [
    astro,
    aws-lambda,
    azure-functions,
    bun,
    connect,
    effect,
    elysia,
    express,
    fastify,
    firebase,
    gcp-functions,
    hapi,
    hono,
    koa,
    mastra,
    nestjs,
    nextjs,
    nitro,
    node,
    nuxt,
    react-router,
    remix,
    solidstart,
    sveltekit,
    tanstackstart-react,
  ]
platformCategory: server
order: 50
---

Koa and Hapi errors are captured by their integrations now, so the error handler setup calls are no longer needed. They are deprecated and will be removed in the next major version.

- `setupKoaErrorHandler(app)`
- `setupHapiErrorHandler(server)`
