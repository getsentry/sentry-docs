1. Install the [sentry-cli](/cli/)

2. Authenticate the sentry-cli by [following these steps](https://docs.sentry.io/cli/configuration/#to-authenticate-manually)

3. Build your app to create an XCArchive (preferred) or IPA

4. Invoke the following CLI command to trigger the upload:

```bash
sentry-cli build upload app.xcarchive \
  --org your-org \
  --project your-project \
  --build-configuration Release
```

5. After the upload has processed, confirm the metadata is correct in the Sentry UI

![Upload metadata =400x](./images/ios-metadata.png)
