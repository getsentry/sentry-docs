```csharp
using Sentry;

SentrySdk.ConfigureScope(scope =>
{
    scope.SetTag("page.locale", "de-at");
});
```

```fsharp
open Sentry

SentrySdk.ConfigureScope(fun scope ->
    scope.SetTag("page.locale", "de-at")
    )
```
