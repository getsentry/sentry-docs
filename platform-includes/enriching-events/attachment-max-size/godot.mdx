```GDScript
extends SentryConfiguration
## Tip: Assign configuration script in the Project Settings.

func _configure(options: SentryOptions):
  options.max_attachment_size = 5 * 1024 * 1024  # 5 MiB
```
