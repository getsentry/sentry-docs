```gdscript
var message := "Player respawned"
var category := "gameplay"
var type := "default"
var data := {
	"player_id": 1,
	"spawn_point": "forest"
}

SentrySDK.add_breadcrumb(
	message,
	category,
	SentrySDK.LEVEL_INFO,
	type,
	data
)
```
