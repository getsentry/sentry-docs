```javascript
Sentry.startSpan({
  op: "test",
  name: "My First Test Span",
}, () => {
  setTimeout(() => {
    try {
      foo();
    } catch (e) {
      Sentry.captureException(e);
    }
  }, 99);
});
```
