<OnboardingOption optionId="profiling" hideForThisOption>

Add the `sentry-ruby` and `sentry-delayed_job` gems to your `Gemfile`:

```ruby {filename:Gemfile}
gem "sentry-ruby"
gem "sentry-delayed_job"
```

</OnboardingOption>

<OnboardingOption optionId="profiling">

Add the `sentry-ruby`, `sentry-delayed_job` and `stackprof` gems to your `Gemfile`:

```ruby {filename:Gemfile}
gem "stackprof"
gem "sentry-ruby"
gem "sentry-delayed_job"
```

</OnboardingOption>
