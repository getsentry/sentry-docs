<OnboardingOption optionId="profiling" hideForThisOption>

Add the `sentry-ruby` and `sentry-resque` gems to your `Gemfile`:

```ruby {filename:Gemfile}
gem "sentry-ruby"
gem "sentry-resque"
```

</OnboardingOption>

<OnboardingOption optionId="profiling">

Add the `sentry-ruby`, `sentry-resque` and `stackprof` gems to your `Gemfile`:

```ruby {filename:Gemfile}
gem "stackprof"
gem "sentry-ruby"
gem "sentry-resque"
```

</OnboardingOption>
