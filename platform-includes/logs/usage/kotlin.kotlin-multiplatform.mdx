```kotlin
import io.sentry.kotlin.multiplatform.Sentry

// Trailing lambda
Sentry.logger.info("User signed up") {
    this["userId"] = user.id
    this["plan"] = "pro"
    this["referrer"] = "google"
}

// Full DSL with parameterized message
Sentry.logger.info {
    message("User %s purchased %s", userId, productName)
    attributes {
        this["userId"] = user.id
        this["plan"] = "pro"
        this["referrer"] = "google"
    }
}
```
