```java
import io.sentry.Sentry;

Sentry.configureScope(scope -> {
  scope.setExtra("key", "value");
});
```

```kotlin {tabTitle: Kotlin}
import io.sentry.Sentry

Sentry.configureScope { scope ->
  scope.setExtra("key", "value")
}
```
