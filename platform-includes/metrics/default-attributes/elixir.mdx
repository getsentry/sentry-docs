The Elixir SDK automatically sets several default attributes on all metrics to provide context and improve debugging:

<Include name="metrics/default-attributes/core" />

### Server Attributes

- `server.address`: The value of the `server_name` option. Unlike errors, metrics don't fall back to the host name, so this attribute is only sent when you set `server_name`.
