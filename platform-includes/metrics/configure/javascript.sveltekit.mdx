Metrics work out of the box by calling `Sentry.init` no further setup is required.

```JavaScript
// hooks.client.js
Sentry.init({
  dsn: '___PUBLIC_DSN___',
});
```

```JavaScript
// hooks.server.js
Sentry.init({
  dsn: '___PUBLIC_DSN___',
});
```
