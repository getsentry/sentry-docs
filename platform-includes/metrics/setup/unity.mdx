Metrics are enabled by default. Once the SDK is initialized, no additional setup is required before you can start emitting metrics.

To turn metrics off, uncheck **Enable Metrics** under **Tools > Sentry > Advanced > Metrics** in the editor, or set `EnableMetrics` to `false` programmatically. See [Options](#options) below.
