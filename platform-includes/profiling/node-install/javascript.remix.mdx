<Note>

Node profiling is available starting in `@sentry/profiling-node` version `0.3.0`.
You have to have the `@sentry/remix` package installed.

</Note>

```bash {tabTitle:npm}
npm install @sentry/remix @sentry/profiling-node --save
```

```bash {tabTitle:yarn}
yarn add @sentry/remix @sentry/profiling-node
```

```bash {tabTitle:pnpm}
pnpm add @sentry/remix @sentry/profiling-node
```
