<Note>

Node profiling is available starting in `@sentry/profiling-node` version `0.3.0`.
You have to have the `@sentry/nuxt` package installed.

</Note>

```bash {tabTitle:npm}
npm install @sentry/nuxt @sentry/profiling-node --save
```

```bash {tabTitle:yarn}
yarn add @sentry/nuxt @sentry/profiling-node
```

```bash {tabTitle:pnpm}
pnpm add @sentry/nuxt @sentry/profiling-node
```
