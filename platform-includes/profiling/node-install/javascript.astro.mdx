<Note>

Node profiling is available starting in `@sentry/profiling-node` version `0.3.0`.
You have to have the `@sentry/astro` package installed.

</Note>

```bash {tabTitle:npm}
npm install @sentry/astro @sentry/profiling-node --save
```

```bash {tabTitle:yarn}
yarn add @sentry/astro @sentry/profiling-node
```

```bash {tabTitle:pnpm}
pnpm add @sentry/astro @sentry/profiling-node
```
