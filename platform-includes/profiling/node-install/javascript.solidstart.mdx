<Note>

Node profiling is available starting in `@sentry/profiling-node` version `0.3.0`.
You have to have the `@sentry/solidstart` package installed.

</Note>

```bash {tabTitle:npm}
npm install @sentry/solidstart @sentry/profiling-node --save
```

```bash {tabTitle:yarn}
yarn add @sentry/solidstart @sentry/profiling-node
```

```bash {tabTitle:pnpm}
pnpm add @sentry/solidstart @sentry/profiling-node
```
