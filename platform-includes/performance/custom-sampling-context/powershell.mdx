```powershell
# Starts the transaction with 
# name = "GET /search"
# op = "http
# CustomSamplingContext = hashtable
$transaction = Start-SentryTransaction "GET /search", "http" @{
    'user_id'        = 42
    'search_results' = @{}
}
```
