`routingInstrumentation` is avaliable for the Vue Router. For more details, see the [Vue Router docs](/platforms/javascript/guides/vue/features/vue-router/).

```JavaScript
import Router from "vue-router";

Vue.use(Router);

const router = new Router({
  // your router configuration
});

Sentry.init({
  Vue,
  dsn: "___PUBLIC_DSN___",
  tracesSampleRate: 1.0,
  integrations: [
    Sentry.browserTracingIntegration({ router }),
  ],
});
```

<Include name="performance/browser-custom-routing-instrumentation.mdx" />
