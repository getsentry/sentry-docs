```dart
import 'package:sentry/sentry.dart';

final transactionContext = SentryTransactionContext(
  'processOrderBatch()',
  'task',
  sampled: true,
);
final transaction = Sentry.startTransactionWithContext(transactionContext);
```
