To use the sampling function, set the <PlatformIdentifier name="traces-sampler" /> option in your `SentryFlutter.init()` to a function that will accept a <PlatformIdentifier name="sampling-context" /> dictionary and return a sample rate between 0 and 1.

In stream mode, <PlatformIdentifier name="traces-sampler" /> receives a different context – read the span's `name` and `attributes` from `samplingContext.spanContext`.

<PlatformContent includePath="performance/traces-sampler-as-sampler" />
