```rust
sentry::init(
    sentry::ClientOptions::new().before_send(|mut event| {
        // Modify event here
        event.server_name = None; // Don't send server name
        Some(event)
    }),
);
```
