```dart
  await SentryFlutter.init(
    (options) {
      options.dsn = '___PUBLIC_DSN___';
      options.ignoreErrors = ["fb_xd_fragment", "error-.*", "^my-error\$"];
      ...
    },
    appRunner: () => runApp(MyApp()),
  );
```
