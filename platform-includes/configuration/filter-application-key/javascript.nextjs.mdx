```javascript {tabTitle:ESM} {filename:next.config.mjs} {2}
export default withSentryConfig(nextConfig, {
  applicationKey: "your-custom-application-key",
});
```

```javascript {tabTitle:CJS} {filename:next.config.js} {2}
module.exports = withSentryConfig(nextConfig, {
  applicationKey: "your-custom-application-key",
});
```

This works for both Webpack and Turbopack builds.
