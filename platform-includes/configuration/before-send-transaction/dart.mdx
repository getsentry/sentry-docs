```dart {2-8}
await Sentry.init((options) {
  options.beforeSendTransaction = (transaction, hint) {
    if (transaction.transaction == 'health-check') {
      return null;
    }

    return transaction;
  };
});
```
