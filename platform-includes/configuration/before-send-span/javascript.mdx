```javascript
Sentry.init({
  dsn: "___PUBLIC_DSN___",

  // Called for all spans
  beforeSendSpan(span) {
    // Modify the span here
    if (span.description === "weird span name") {
      span.description = "useful span name";
      span.data = {
        ...span.data,
        myExtraAttribute: true,
      };
    }

    return span;
  },
});
```
