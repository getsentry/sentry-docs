```dart
import 'package:sentry/sentry.dart';

Future<void> main() async {
  await Sentry.init((options) => options.release = 'my-project-name@2.3.12+12'); // packageName@versionName+buildNumber
}
```
