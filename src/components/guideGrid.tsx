import { Note } from "./note";

type Props = {
  className?: string;
  platform?: string;
};

export function GuideGrid({platform, className}: Props) {
  return <Note>The `GuideGrid` component is under construction.</Note>;
}
