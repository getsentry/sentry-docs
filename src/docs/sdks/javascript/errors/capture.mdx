---
title: Error and Message Capture
excerpt: ""
---
Sentry's SDK hooks into your runtime environment and automatically reports errors, exceptions, and rejections.

## Event Capture

The most common form of capturing is to capture errors. What can be captured as an error varies by platform. In general, if you have something that looks like an exception it can be captured. For some SDKs you can also omit the argument to `capture_exception` and Sentry will attempt to capture the current exception. It can also be useful to manually report errors or messages to Sentry.

Separately to capturing you can also record the breadcrumbs that lead up to an event. Breadcrumbs are different from events: they will not create an event in Sentry, but will be buffered until the next event is sent. Learn more about breadcrumbs in our [Breadcrumbs documentation]((sdks/javascript/errors/breadcrumbs/)).

### Manually Capturing Errors

In JavaScript, you can pass an error object to `captureException()` to get it captured as an event. It’s also possible to throw strings as errors, in which case no traceback can be recorded.

```JavaScript
try {
  aFunctionThatMightFail();
} catch (err) {
  Sentry.captureException(err);
}
```

## Enable Message Capture

Another common operation is to capture a bare message. A message is textual information that should be sent to Sentry. Typically messages are not emitted, but they can be useful for some teams.

```JavaScript
Sentry.captureMessage('Something went wrong');
```

## Next Steps:

 - [Return to **Getting Started**](/sdks/javascript/)
 - [Return to the main enriching event data page](intro)
