---
title: Override the Fingerprint
sidebar_order: 4
---

By default, Sentry runs one of our built-in grouping algorithms to generate a fingerprint based on information available within the event such as `stacktrace`, `exception`, and `message`.

You can override the default grouping by passing the `fingerprint` attribute an array of strings. The following extra values are available for fingerprints:

`{{ default }}`

: the default fingerprint values

`{{ transaction }}`

: the event's transaction name

`{{ function }}`

: the event’s function name

`{{ type }}`

: the event’s exception type

`{{ module }}`

: the event’s module name

`{{ package }}`

: the event’s package name

If you wish to append information, thus making the grouping slightly less aggressive, you can add the special string `{{ default }}` as one of the items.

While not generally useful, this minimal example puts all events into a fixed issue/group:

<PlatformContent includePath="set-fingerprint/static" />

<PlatformContent includePath="set-fingerprint/rpc"><markdown>

A real world use case might be customizing a fingerprint based on an API call to split the groups based on the available information:

</markdown></PlatformContent>

<PlatformContent includePath="set-fingerprint/database-connection"><markdown>

Maybe you have a database connection error that should all group into the same issue:

</markdown></PlatformContent>
