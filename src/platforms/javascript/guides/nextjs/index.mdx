---
title: React
sdk: sentry.javascript.react
redirect_from:
  - /platforms/javascript/react/
  - /clients/javascript/integrations/react/
  - /sdks/react/
  - /platforms/react/
description: "Learn how to use Sentry's NextJS SDK and how it automatically reports errors and exceptions in your application."
---

To use Sentry with your NextJS application, you will need to use Sentry’s NextJS SDK: `@sentry/nextjs`.

## Install

Add the Sentry SDK as a dependency using `npm` or `yarn`:

```bash {tabTitle:npm}
npm install --save @sentry/nextjs
```

```bash {tabTitle:Yarn}
yarn add @sentry/nextjs
```
