---
title: Offline Caching
description: "Learn how the SDK handles offline caching."
sidebar_order: 200
---

This is enabled by default. When a device is unable to send an event, it is cached and we send a report once we receive another event. The SKD uses `Application.persistentDataPath` as base path for caching.
