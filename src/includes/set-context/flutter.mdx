```dart
import 'package:sentry/sentry.dart';

final sentry = SentryClient(
  environmentAttributes: const Event(
    extra: {
      'name': 'Mighty Fighter',
      'age': 19,
      'attack_type': 'melee',
    }
  )
);
```
