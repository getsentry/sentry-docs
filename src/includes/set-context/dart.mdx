```dart
import 'package:sentry/sentry.dart';

void main() {
  final person = {
    'name': 'Mighty Fighter',
    'age': '19',
  };
  Sentry.configureScope((scope) => scope.setContexts('person', person));
}
```
