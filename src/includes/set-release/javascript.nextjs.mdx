We'll automatically bind the release value for you when using Vercel. To manually set the value, use the `NEXT_PUBLIC_SENTRY_RELEASE` environment variable:

```shell
NEXT_PUBLIC_SENTRY_RELEASE="my-project-name@2.3.12"
```
