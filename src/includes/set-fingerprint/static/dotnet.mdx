```csharp
using (SentrySdk.Init(o =>
{
    o.BeforeSend = @event =>
    {
        @event.SetFingerprint(
            new []
            {
                "my-view-function",
            }
        );

        return @event;
    };
}
```
