```java {tabTitle: Java}
import java.util.Arrays;
import io.sentry.Sentry;

Sentry.configureScope(scope -> {
    scope.setFingerprint(Arrays.asList("auth-error"));
});
```

```kotlin {tabTitle: Kotlin}
import io.sentry.Sentry

Sentry.configureScope {
    it.fingerprints = listOf("auth-error")
}
```
