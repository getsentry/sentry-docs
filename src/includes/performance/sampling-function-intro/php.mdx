To use the sampling function, set the <PlatformIdentifier name="traces-sampler" /> option to a closure that will accept a `\Sentry\Tracing\SamplingContext` object and return a sample rate between 0 and 1. For example:

<PlatformContent includePath="performance/traces-sampler-as-filter" />
