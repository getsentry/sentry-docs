```java {tabTitle:Java}
import io.sentry.ITransaction;
import io.sentry.Sentry;
import io.sentry.TransactionContext;

TransactionContext transactionContext = new TransactionContext("GET /search");
transactionContext.setSampled(true);
ITransaction transaction = Sentry.startTransaction(transactionContext);
```

```kotlin {tabTitle:Kotlin}
import io.sentry.Sentry
import io.sentry.TransactionContext

val transactionContext = TransactionContext("GET /search")
transactionContext.setSampled(true)
val transaction = Sentry.startTransaction(transactionContext)
```
