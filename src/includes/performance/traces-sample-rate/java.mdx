```java {tabTitle:Java}
import io.sentry.Sentry;

Sentry.init(
  options -> {
    ...
    options.setTracesSampleRate(0.2);
  });
```

```kotlin {tabTitle:Kotlin}
import io.sentry.Sentry

Sentry.init {
    ...
    it.tracesSampleRate = 0.2
}
```
