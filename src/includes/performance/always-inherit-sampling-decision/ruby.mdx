```ruby
config.traces_sampler = lambda do |tracing_context|
  parent_sampled = tracing_context[:parent_sampled]

  if !parent_sampled.nil?
    parent_sampled
  else
    # the rest of sampling logic
  end
end
```
