```java
  import 'package:sentry/sentry.dart';

  void scope(Scope scope) {
    scope.setTag('my-tag', 'my value');
    final user = User(id: '42', email: 'john.doe@example.com');
    scope.user = user;
  }

  Sentry.configureScope(scope);
```
