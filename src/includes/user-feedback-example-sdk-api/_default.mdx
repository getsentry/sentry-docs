When a user experiences an error, Sentry provides the ability to collect additional feedback.
You can create a form to collect the user input in your prefered framework, and use the SDK's API to send the information to Sentry.
