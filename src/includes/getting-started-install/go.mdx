Install our Go SDK using [`go get`](https://golang.org/cmd/go/#hdr-Module_aware_go_get), and if using Go Modules, it will pick up the latest tagged version:

```bash
go get github.com/getsentry/sentry-go
```
