To add Sentry to your Clojure project you just need to add a new dependency to your `deps.edn`, `project.clj` or `boot.clj` file:

```clojure
:deps {io.sentry/sentry {:mvn/version "3.2.0"}
       io.sentry/sentry-clj {:mvn/version "3.1.135"}}
```
