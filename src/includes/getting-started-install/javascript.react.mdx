Add the Sentry's React SDK as a dependency using `yarn` or `npm`:

```bash {tabTitle: ESM}
# Using npm
npm install --save @sentry/react @sentry/tracing
# Using yarn
yarn add @sentry/react @sentry/tracing
