<Note>

Note that capturing a screenshot in the Unity SDK requires running on the main thread.

</Note>

Screenshots may contain <PlatformLink to="/data-management/sensitive-data/">PII</PlatformLink> and is an opt-in feature. You can find the toggle on the `Enrichment` tab of the editor configuration window.
