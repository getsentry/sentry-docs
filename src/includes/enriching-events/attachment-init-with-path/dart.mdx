<Note>

Adding an attachment via path is not available on web platforms.

</Note>

```dart
import 'package:sentry/sentry_io.dart';

final attachment = IoSentryAttachment.fromPath("your/path/file.log");
```