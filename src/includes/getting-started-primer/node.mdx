<Note>
<markdown>

_Sentry's Node SDK enables automatic reporting of errors, exceptions, and transactions._

Our Node SDK supports all recent versions, and integrates well with a variety of popular frameworks and packages. It gives developers the ability to see Node source code at each frame instead and get proper asynchronous context tracking — like thread-local storage — in a way that fits Node’s concurrency model.

</markdown>
</Note>
