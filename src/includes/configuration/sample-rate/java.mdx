```java {tabTitle: Java}
import io.sentry.Sentry;

Sentry.init(
  options -> {
    options.setSampleRate(0.25);
  });
```

```kotlin {tabTitle: Kotlin}
import io.sentry.Sentry

Sentry.init {
  it.sampleRate = 0.25
}
```
