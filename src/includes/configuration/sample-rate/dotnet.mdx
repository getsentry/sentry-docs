```csharp
using Sentry;

using (SentrySdk.Init(o =>
{
    o.SampleRate = 0.25; // Capture 25%
})
{
    // app code here
}
```

```fsharp
open Sentry

use __ = SentrySdk.Init(fun o ->
    o.SampleRate <- 0.25  // Capture 25%
    ())

// app code here
```
