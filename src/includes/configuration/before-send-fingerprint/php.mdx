```php
\Sentry\init([
  'dsn' => '___DSN___',
  'before_send' => function (\Sentry\Event $event, ?\Sentry\EventHint $hint): ?\Sentry\Event {
    if ($hint !== null && $hint->exception instanceof MyException) {
      $event->setFingerprint(['my-exception'])
    }
    
    return $event;
  },
]);
```
