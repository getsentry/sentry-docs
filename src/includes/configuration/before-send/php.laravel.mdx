In the Laravel config a function can be used to modify the event or return a completely new one. If you return `null`, the event will be discarded.

```php {filename:config/sentry.php}
'before_send' => function (\Sentry\Event $event): ?\Sentry\Event {
  return $event;
},
```
