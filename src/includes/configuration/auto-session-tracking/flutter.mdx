```dart
import 'package:flutter/widgets.dart';
import 'package:sentry_flutter/sentry_flutter.dart';

void main() async {
  await SentryFlutter.init(
    (options) => options.enableAutoSessionTracking = true,
    () {
      // Run your App
      runApp(MyApp());
    },
  );
}
```
