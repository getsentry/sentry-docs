The Xamarin SDK automatically shuts down and waits `ShutdownTimeout` seconds before that happens
when the `Init`'s return:

```csharp
	SentryXamarin.Init(...));
    // App code
```

In case of an unhandled exception that will crash the app, the SDK automatically disposes itself.
