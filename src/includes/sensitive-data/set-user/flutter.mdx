```dart
  import 'package:sentry/sentry.dart';

  void scope(Scope scope) {
    final user = User(id: clientUser.id, username: clientUser.username);
    scope.user = user;
  }

  Sentry.configureScope(scope);
```
