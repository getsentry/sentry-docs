<SignInNote />

```javascript {tabTitle:ESM}
import * as Sentry from "@sentry/bun";

Sentry.init({
  dsn: "___PUBLIC_DSN___",
  tracesSampleRate: 1.0,
});
```

Once configured, you can capture exceptions manually, like the next example shows.
