```php {filename:config/sentry.php}
// This logs messages to the provided file path
'logger' => new DebugFileLogger(filePath: app()-> . 'sentry.log'),

// This logs messages to stdout

'logger' => new DebugStdOutLogger(filePath: storage_path('logs/sentry.log')),
```
