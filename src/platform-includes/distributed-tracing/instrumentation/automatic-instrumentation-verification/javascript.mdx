## Verification

If you make outgoing XHR/fetch requests from your project to other services, check if the HTTP headers `sentry-trace` and `baggage` are present in the request. If so distributed tracing is working.
