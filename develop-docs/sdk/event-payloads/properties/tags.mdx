`tags`:

: _Optional_. A map or list of tags for this event. Each tag must be less than 200 characters.

  ```json
  {
    "tags": {
      "ios_version": "4.0",
      "context": "production"
    }
  }
  ```