`op`

: _Recommended_. Short code identifying the type of operation the span is measuring.

For more details, see <Link to="/sdk/telemetry/transactions/#span-operations">Sentry's conventions around span operations</Link>.

  ```json
  {
    "op": "db.query"
  }
  ```
