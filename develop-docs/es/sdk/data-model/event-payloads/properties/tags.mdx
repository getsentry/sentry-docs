`tags`

: *Opcional*. Un mapa o una lista de etiquetas para este evento. Las etiquetas deben tener valores de tipo cadena y cada una debe tener menos de 200 caracteres.

```json
  {
    "tags": {
      "ios_version": "4.0",
      "context": "production"
    }
  }
```
