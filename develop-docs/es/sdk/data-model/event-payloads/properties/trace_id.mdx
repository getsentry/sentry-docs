`trace_id`:

: *Obligatorio*. Determina a qué `trace` pertenece el span. El valor debe ser 16 bytes aleatorios codificados como una cadena hexadecimal (32 caracteres de longitud).

```json
  {
    "id_de_traza": "1e57b752bc6e4544bbaa246cd1d05dee"
  }
```
