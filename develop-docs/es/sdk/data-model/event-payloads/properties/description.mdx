`description`

: *Opcional*. Descripción más detallada de la operación del span, que identifica de forma única el span, pero se mantiene consistente entre las distintas instancias del span.

```json
  {
    "description": "SELECT * FROM users WHERE last_active < DATE_SUB(CURRENT_DATE, INTERVAL 1 YEAR)`"
  }
```
