---
title: Muestreo
---

<Alert level="warning">
  🚧 Este documento está en desarrollo.
</Alert>

<Alert>
  Este documento utiliza términos clave como “MUST”, “SHOULD” y “MAY”, tal como se definen en la [RFC 2119](https://www.ietf.org/rfc/rfc2119.txt), para indicar niveles de requisito.
</Alert>

Cualquier API expuesta al usuario para muestrear spans MUST cumplir con los siguientes principios de diseño:

- El muestreo MUST realizarse únicamente en un span raíz
- Las API están optimizadas para la integridad del trace
- Las API están optimizadas para tomar decisiones de muestreo concluyentes

<div id="sample-with-tracessamplerate">
  ## Ejemplo con `tracesSampleRate`
</div>

El SDK se inicializa automáticamente con un `tracesSampleRate` de `0.0`.
Al iniciar un span raíz, la tasa configurada se compara con un número aleatorio entre `0.0` y `1.0` para decidir si ese span raíz se muestrea o no.

<div id="sample-with-tracessampler">
  ## Ejemplo con `tracesSampler`
</div>

Si el SDK está configurado con un `tracesSampler`, `tracesSampleRate` deja de aplicarse.

La función de devolución de llamada de `tracesSampler` DEBE recibir suficientes argumentos por parte de los usuarios para definir sus propias reglas de muestreo.
ESTO PUEDE incluir, entre otros, ciertos atributos del span raíz, como los encabezados HTTP.
El valor de retorno de `tracesSampler` es un número de punto flotante entre `0.0` y `1.0`.

Si no se configura un `tracesSampler`, una decisión de muestreo propagada a través de traceparent tiene prioridad sobre `tracesSampleRate`. Este comportamiento PUEDE deshabilitarse definiendo un `tracesSampler`.